import { CurrenyLocalPipe } from './curreny-local.pipe';

describe('CurrenyLocalPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrenyLocalPipe();
    expect(pipe).toBeTruthy();
  });
});
